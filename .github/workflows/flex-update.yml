name: Update Flex endpoint

on:
    push:
        branches:
            - master-serverless

defaults:
    run:
        shell: bash

jobs:
    call-flex-update:
        uses: symfony/recipes/.github/workflows/callable-flex-update.yml@main
        with:
            branch: master-serverless
